package com.kaveenk.onlydragons.domain.stats;

/** Applied after base values: flat additions, summed percentage points, then ordered factors. */
public enum ModifierOperation {
    FLAT, ADDITIVE_PERCENT, MULTIPLIER
}
